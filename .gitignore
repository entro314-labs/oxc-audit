node_modules/
dist/
*.log
.DS_Store
coverage/
.vscode/
.idea/
*.tsbuildinfo
examples/
.nope
# Migration lock left behind only if a run is killed mid-flight
.biome-to-oxc.lock

# Audit lock, left behind only if a run is killed mid-flight
.oxc-audit.lock
