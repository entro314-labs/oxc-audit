{
	"name": "@entro314-labs/oxlint-audit-plugins",
	"version": "1.3.1",
	"private": false,
	"description": "Oxlint rules extracted from the version-locked stack audit prompts: deprecated APIs, migration residue and security footguns across Next.js/React, the data layer, AI SDK/payment integrations and the JS toolchain.",
	"type": "module",
	"exports": {
		"./next-react": "./dist/next-react/index.mjs",
		"./data-layer": "./dist/data-layer/index.mjs",
		"./ai-integrations": "./dist/ai-integrations/index.mjs",
		"./ts-tooling": "./dist/ts-tooling/index.mjs",
		"./slop-stop": "./dist/slop-stop/index.mjs",
		"./supabase": "./dist/supabase/index.mjs",
		"./web-security": "./dist/web-security/index.mjs"
	},
	"files": [
		"dist",
		"oxlintrc.audit.json"
	],
	"scripts": {
		"build": "tsdown",
		"check": "pnpm lint && pnpm typecheck && pnpm test && pnpm test:fixtures && pnpm check:skill-assets",
		"check:skill-assets": "node scripts/sync-skill-assets.mjs --check",
		"lint": "oxlint src",
		"prepublishOnly": "pnpm check && pnpm build",
		"sync:skill-assets": "node scripts/sync-skill-assets.mjs",
		"test": "for f in src/*/rules/*.test.ts; do echo \"  $f\" && tsx \"$f\" || exit 1; done",
		"test:fixtures": "oxlint -c oxlintrc.audit.json --deny-warnings fixtures > /dev/null 2>&1 && echo 'fixtures produced no findings — the integration fixture has regressed' && exit 1 || echo 'fixtures still trip the plugins'",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@oxlint/plugins": "^1.81.0"
	},
	"devDependencies": {
		"@types/node": "^26.2.0",
		"oxlint": "^1.81.0",
		"oxlint-tsgolint": "^7.0.2001",
		"tsdown": "^0.22.14",
		"tsx": "^4.23.12",
		"typescript": "next"
	},
	"packageManager": "pnpm@11.22.0",
	"license": "MIT",
	"engines": {
		"node": ">=20.19.0"
	}
}
